{
    "filename": "/home/eidtech/gcc/5.mp4",
    "RoiRect": {
        "x": 270,
        "y": 10,
        "width": 300,
        "height": 700
    }
}


// ``### 编辑方法
// 1. **打开文件**
//    - 使用文本编辑器（如Notepad++、VSCode、Sublime Text等）打开`config.json`文件。

// 2. **设置视频文件路径**
//    - 修改`filename`字段，设置为你的视频文件路径。例如：
//      ```json
//      "filename": "C:/Users/YourName/Videos/example.mp4"
//      ```

// 3. **设置ROI区域**
//    - 修改`RoiRect`字段，设置感兴趣区域的坐标和尺寸。例如：
//      ```json
//      "RoiRect": {
//          "x": 100,
//          "":y 200,
//          "width": 400,
//          "height": 300
//      }
//      ```

// 4. **保存文件**
//    - 保存对`config.json`文件的修改。

// ### 注意事项
// - **确保路径正确**：视频文件路径必须正确，否则程序无法找到文件。
// - **确保ROI区域有效**：ROI区域的坐标和尺寸不能超出视频帧的范围。
// - **格式正确**：确保JSON文件格式正确，避免语法错误。

// ### 示例说明
// - `filename`：视频文件的路径。
// - `RoiRect`：感兴趣区域的坐标和尺寸，其中：
//   - `x`：ROI区域左上角的x坐标。
//   - `y`：ROI区域左上角的y坐标。
//   - `width`：ROI区域的宽度。
//   - `height`：ROI区域的高度。

// 通过编辑`config.json`文件，你可以灵活地调整程序的输入参数，而无需修改代码本身。这使得程序更加通用和易于维护。